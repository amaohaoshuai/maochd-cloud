mirrors:
  "hub.maochd.com":
    endpoint:
      - "https://hub.maochd.com"
configs:
  "hub.maochd.com":
    auth:
      # 这是私有镜像仓库的用户名
      username: admin
      # 这是私有镜像仓库的密码
      password: 123456
    tls:
      # 跳过tls校验
      insecure_skip_verify: true